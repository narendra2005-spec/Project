import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.inclass.app',
  appName: 'InClass',
  webDir: 'dist',
  server: {
    hostname: 'gen-lang-client-0326695470.firebaseapp.com',
    androidScheme: 'https'
  }
};

export default config;
